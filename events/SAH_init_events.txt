# ============================================================
# Stakuyi's Alt History - Initialisation Events
# Namespace: SAH_init
# ============================================================

add_namespace = SAH_init

country_event = {
	id = SAH_init.1
	title = SAH_init.1.t
	desc = SAH_init.1.d
	picture = GFX_report_event_generic_read_write

	is_triggered_only = yes
	hidden = yes

	immediate = {
		if = { limit = { date > 1938.1.1 }
			SAH_adjust_1939_units = yes
			SAH_purge_1939_ideas = yes
		}

		# --- AUTO-GENERATED SCENARIO STARTS ---
		USA = {
			if = {
				limit = { has_game_rule = { rule = SAH_usa_scenario_start_rule option = american_caesar } }
				# Rewards for USA_reestablish_the_gold_standard
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for USA_america_first_committee
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for USA_neutrality_to_the_end
				add_ideas = USA_strict_neutrality
				# Rewards for USA_the_american_caesar
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_usa_scenario_start_rule option = fascist_menace } }
				# Rewards for USA_america_first
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for USA_paramilitary_training
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for USA_corporate_statism
				random_owned_controlled_state = {
                limit = { free_building_slots = { building = industrial_complex size > 0 } }
						if = { limit = { date < 1938.1.1 }
                add_building_construction = { type = industrial_complex level = 3 instant_build = yes }
						}
            }
				# Rewards for USA_ally_with_the_silver_shirts
				add_popularity = { ideology = fascism popularity = 0.15 }
				# Rewards for USA_voter_registration_act
				add_popularity = { ideology = fascism popularity = 0.2 }
				# Rewards for USA_the_fascist_menace
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_usa_scenario_start_rule option = communist } }
				# Rewards for USA_suspend_the_persecution
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for USA_unholy_alliance
				add_popularity = { ideology = communism popularity = 0.15 }
				# Rewards for USA_democratic_socialism
				add_popularity = { ideology = communism popularity = 0.2 }
				# Rewards for USA_the_peoples_republic
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_usa_scenario_start_rule option = giant_wakes } }
				# Rewards for USA_continue_the_new_deal
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for USA_neutrality_act
				add_ideas = neutrality_idea
				# Rewards for USA_arsenal_of_democracy
				add_ideas = USA_arsenal_of_democracy
				# Rewards for USA_wpa
				random_owned_controlled_state = {
                limit = { free_building_slots = { building = industrial_complex size > 0 } }
						if = { limit = { date < 1938.1.1 }
                add_building_construction = { type = industrial_complex level = 2 instant_build = yes }
						}
            }
				# Rewards for USA_war_production_board
				add_ideas = USA_war_production_board
				# Rewards for USA_war_department_expansion
				random_owned_controlled_state = {
                limit = { free_building_slots = { building = arms_factory size > 0 } }
						if = { limit = { date < 1938.1.1 }
                add_building_construction = { type = arms_factory level = 2 instant_build = yes }
						}
            }
				# Rewards for USA_selective_service_act
				add_ideas = USA_selective_service_idea
				# Rewards for USA_the_giant_wakes
				remove_ideas = USA_great_depression
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		GER = {
			if = {
				limit = { has_game_rule = { rule = SAH_ger_scenario_start_rule option = danzig_or_war } }
				# Rewards for GER_oppose_hitler
				add_popularity = { ideology = neutrality popularity = 0.2 }
				# Rewards for GER_communist_uprising
				add_popularity = { ideology = communism popularity = 0.3 }
				# Rewards for GER_befriend_poland
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ger_scenario_start_rule option = return_of_kaiser } }
				# Rewards for GER_communist_uprising
				add_popularity = { ideology = communism popularity = 0.3 }
				# Rewards for GER_oppose_hitler
				add_popularity = { ideology = neutrality popularity = 0.2 }
				# Rewards for GER_reestablish_free_elections
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for GER_revive_the_kaiserreich
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rewards for GER_return_of_the_kaiser
				add_ideas = GER_kaiser_returns
                        add_stability = 0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ger_scenario_start_rule option = communist_uprising } }
				# Rewards for GER_oppose_hitler
				add_popularity = { ideology = neutrality popularity = 0.2 }
				# Rewards for GER_communist_uprising
				add_popularity = { ideology = communism popularity = 0.3 }
				# Rewards for GER_the_workers_republic
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ger_scenario_start_rule option = anti_soviet_pact } }
				# Rewards for GER_communist_uprising
				add_popularity = { ideology = communism popularity = 0.3 }
				# Rewards for GER_oppose_hitler
				add_popularity = { ideology = neutrality popularity = 0.2 }
				# Rewards for GER_revive_the_kaiserreich
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rewards for GER_reestablish_free_elections
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for GER_support_democracy_in_europe
				add_popularity = { ideology = democratic popularity = 0.15 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		SOV = {
			if = {
				limit = { has_game_rule = { rule = SAH_sov_scenario_start_rule option = historical_stalin } }
				# Rewards for SOV_beaten_but_not_defeated
				add_stability = -0.2
                        add_popularity = { ideology = neutrality popularity = 0.2 }
                        add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for SOV_the_center
				add_ideas = SOV_stalin_cult_of_personality
				# Rewards for SOV_the_great_purge
				add_stability = -0.1
			}
			if = {
				limit = { has_game_rule = { rule = SAH_sov_scenario_start_rule option = permanent_revolution } }
				# Rewards for SOV_beaten_but_not_defeated
				add_stability = -0.2
                        add_popularity = { ideology = neutrality popularity = 0.2 }
                        add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for SOV_the_center
				add_ideas = SOV_stalin_cult_of_personality
				# Rewards for SOV_permanent_revolution
				add_ideas = SOV_permanent_revolution_idea
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_sov_scenario_start_rule option = new_economic_policy } }
				# Rewards for SOV_beaten_but_not_defeated
				add_stability = -0.2
                        add_popularity = { ideology = neutrality popularity = 0.2 }
                        add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for SOV_the_center
				add_ideas = SOV_stalin_cult_of_personality
				# Rewards for SOV_the_new_economic_policy
				add_ideas = SOV_nep_idea
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_sov_scenario_start_rule option = tsarist_restored } }
				# Rewards for SOV_the_center
				add_ideas = SOV_stalin_cult_of_personality
				# Rewards for SOV_beaten_but_not_defeated
				add_stability = -0.2
                        add_popularity = { ideology = neutrality popularity = 0.2 }
                        add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for SOV_the_fascist_destiny
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rewards for SOV_a_new_russia
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for SOV_the_romanovs_restored
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_sov_scenario_start_rule option = republic } }
				# Rewards for SOV_the_center
				add_ideas = SOV_stalin_cult_of_personality
				# Rewards for SOV_beaten_but_not_defeated
				add_stability = -0.2
                        add_popularity = { ideology = neutrality popularity = 0.2 }
                        add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for SOV_the_fascist_destiny
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rewards for SOV_the_romanovs_restored
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rewards for SOV_a_new_russia
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for SOV_the_duma
				add_stability = 0.15
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_sov_scenario_start_rule option = fascist_destiny } }
				# Rewards for SOV_the_center
				add_ideas = SOV_stalin_cult_of_personality
				# Rewards for SOV_beaten_but_not_defeated
				add_stability = -0.2
                        add_popularity = { ideology = neutrality popularity = 0.2 }
                        add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for SOV_the_romanovs_restored
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rewards for SOV_a_new_russia
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for SOV_the_fascist_destiny
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rewards for SOV_the_russian_fascist_party
				add_ideas = SOV_russian_fascism
                        add_stability = 0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		ENG = {
			if = {
				limit = { has_game_rule = { rule = SAH_eng_scenario_start_rule option = churchill } }
				# Rewards for ENG_a_change_in_course
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for ENG_organize_the_blackshirts
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for ENG_concessions_to_the_trade_unions
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for ENG_steady_as_she_goes
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for ENG_rearmament
				add_ideas = ENG_rearmament_idea
				# Rewards for ENG_the_shadow_scheme
				random_owned_controlled_state = {
                limit = { free_building_slots = { building = industrial_complex size > 0 } }
						if = { limit = { date < 1938.1.1 }
                add_building_construction = { type = industrial_complex level = 2 instant_build = yes }
						}
            }
				# Rewards for ENG_early_mobilization
				add_ideas = early_mobilization
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_eng_scenario_start_rule option = kings_party } }
				# Rewards for ENG_organize_the_blackshirts
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for ENG_steady_as_she_goes
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for ENG_concessions_to_the_trade_unions
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for ENG_a_change_in_course
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for ENG_the_kings_party
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_eng_scenario_start_rule option = mosley_fascist } }
				# Rewards for ENG_a_change_in_course
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for ENG_steady_as_she_goes
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for ENG_concessions_to_the_trade_unions
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for ENG_organize_the_blackshirts
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for ENG_march_on_london
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_eng_scenario_start_rule option = communist_britain } }
				# Rewards for ENG_a_change_in_course
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for ENG_organize_the_blackshirts
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for ENG_steady_as_she_goes
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for ENG_concessions_to_the_trade_unions
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for ENG_the_communist_alternative
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		FRA = {
			if = {
				limit = { has_game_rule = { rule = SAH_fra_scenario_start_rule option = french_commune } }
				# Rewards for FRA_utilize_the_leagues
				add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for FRA_form_the_popular_front
				add_popularity = { ideology = democratic popularity = 0.1 }
                        add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for FRA_national_bloc
				add_popularity = { ideology = democratic popularity = 0.2 }
				# Rewards for FRA_invite_communist_ministers
				add_popularity = { ideology = communism popularity = 0.2 }
                        add_stability = -0.05
				# Rewards for FRA_the_french_commune
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_fra_scenario_start_rule option = legitimist } }
				# Rewards for FRA_form_the_popular_front
				add_popularity = { ideology = democratic popularity = 0.1 }
                        add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for FRA_utilize_the_leagues
				add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for FRA_laissez_faire
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rewards for FRA_orleanist_restoration
				set_politics = { ruling_party = neutrality elections_allowed = no }
                        add_stability = 0.1
				# Rewards for FRA_bonapartist_restoration
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rewards for FRA_legitimist_restoration
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_fra_scenario_start_rule option = bonapartist } }
				# Rewards for FRA_form_the_popular_front
				add_popularity = { ideology = democratic popularity = 0.1 }
                        add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for FRA_utilize_the_leagues
				add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for FRA_laissez_faire
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rewards for FRA_legitimist_restoration
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rewards for FRA_orleanist_restoration
				set_politics = { ruling_party = neutrality elections_allowed = no }
                        add_stability = 0.1
				# Rewards for FRA_bonapartist_restoration
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_fra_scenario_start_rule option = latin_entente } }
				# Rewards for FRA_form_the_popular_front
				add_popularity = { ideology = democratic popularity = 0.1 }
                        add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for FRA_utilize_the_leagues
				add_popularity = { ideology = fascism popularity = 0.1 }
                        add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for FRA_laissez_faire
				set_politics = { ruling_party = fascism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		JAP = {
			if = {
				limit = { has_game_rule = { rule = SAH_jap_scenario_start_rule option = strike_south } }
				# Rewards for JAP_the_unthinkable_option
				add_popularity = { ideology = democratic popularity = 0.15 }
                        add_popularity = { ideology = communism popularity = 0.15 }
				# Rewards for JAP_support_the_kodoha_faction
				add_popularity = { ideology = neutrality popularity = 0.15 }
				# Rewards for JAP_purge_the_kodoha_faction
				add_popularity = { ideology = fascism popularity = 0.15 }
				# Rewards for JAP_nationalize_state_shinto
				add_ideas = JAP_state_shinto_idea
				# Rewards for JAP_strike_south_doctrine
				add_ideas = JAP_strike_south
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_jap_scenario_start_rule option = showa_restoration } }
				# Rewards for JAP_the_unthinkable_option
				add_popularity = { ideology = democratic popularity = 0.15 }
                        add_popularity = { ideology = communism popularity = 0.15 }
				# Rewards for JAP_purge_the_kodoha_faction
				add_popularity = { ideology = fascism popularity = 0.15 }
				# Rewards for JAP_support_the_kodoha_faction
				add_popularity = { ideology = neutrality popularity = 0.15 }
				# Rewards for JAP_showa_restoration
				set_politics = { ruling_party = neutrality elections_allowed = no }
                        add_ideas = JAP_showa_restoration_idea
                        add_stability = 0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_jap_scenario_start_rule option = democratic_diet } }
				# Rewards for JAP_purge_the_kodoha_faction
				add_popularity = { ideology = fascism popularity = 0.15 }
				# Rewards for JAP_support_the_kodoha_faction
				add_popularity = { ideology = neutrality popularity = 0.15 }
				# Rewards for JAP_the_unthinkable_option
				add_popularity = { ideology = democratic popularity = 0.15 }
                        add_popularity = { ideology = communism popularity = 0.15 }
				# Rewards for JAP_the_japanese_communist_party
				set_politics = { ruling_party = communism elections_allowed = no }
                        add_stability = -0.1
				# Rewards for JAP_restore_the_diet
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_jap_scenario_start_rule option = communist_japan } }
				# Rewards for JAP_purge_the_kodoha_faction
				add_popularity = { ideology = fascism popularity = 0.15 }
				# Rewards for JAP_support_the_kodoha_faction
				add_popularity = { ideology = neutrality popularity = 0.15 }
				# Rewards for JAP_the_unthinkable_option
				add_popularity = { ideology = democratic popularity = 0.15 }
                        add_popularity = { ideology = communism popularity = 0.15 }
				# Rewards for JAP_restore_the_diet
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for JAP_the_japanese_communist_party
				set_politics = { ruling_party = communism elections_allowed = no }
                        add_stability = -0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		ITA = {
			if = {
				limit = { has_game_rule = { rule = SAH_ita_scenario_start_rule option = pact_of_steel } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ita_scenario_start_rule option = latin_bloc } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ita_scenario_start_rule option = kingdom_of_god } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ita_scenario_start_rule option = republic_of_italy } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ita_scenario_start_rule option = communist_italy } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		POL = {
			if = {
				limit = { has_game_rule = { rule = SAH_pol_scenario_start_rule option = april_constitution } }
				# Rewards for POL_the_polish_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for POL_the_regency_council
				add_stability = -0.1
				# Rewards for POL_the_falanga
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for POL_the_morges_front
				set_politics = { ruling_party = democratic }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for POL_the_castle
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for POL_april_constitution
				add_stability = 0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_pol_scenario_start_rule option = falanga } }
				# Rewards for POL_the_polish_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for POL_the_regency_council
				add_stability = -0.1
				# Rewards for POL_the_morges_front
				set_politics = { ruling_party = democratic }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for POL_the_castle
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for POL_the_falanga
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_pol_scenario_start_rule option = comintern } }
				# Rewards for POL_the_regency_council
				add_stability = -0.1
				# Rewards for POL_the_falanga
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for POL_the_morges_front
				set_politics = { ruling_party = democratic }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for POL_the_castle
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for POL_the_polish_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_pol_scenario_start_rule option = regency_council } }
				# Rewards for POL_the_polish_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for POL_the_falanga
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for POL_the_morges_front
				set_politics = { ruling_party = democratic }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for POL_the_castle
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rewards for POL_the_regency_council
				add_stability = -0.1
				# Rewards for POL_invite_the_king
				set_politics = { ruling_party = neutrality elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		CAN = {
			if = {
				limit = { has_game_rule = { rule = SAH_can_scenario_start_rule option = zombies } }
				# Rewards for CAN_bring_the_unions_to_the_table
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for CAN_patriation
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for CAN_swastika_clubs
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for CAN_send_in_the_zombies
				add_manpower = 50000
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_can_scenario_start_rule option = fascist_canada } }
				# Rewards for CAN_bring_the_unions_to_the_table
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for CAN_patriation
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for CAN_swastika_clubs
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for CAN_national_unity_party
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_can_scenario_start_rule option = communist_canada } }
				# Rewards for CAN_swastika_clubs
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for CAN_patriation
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for CAN_bring_the_unions_to_the_table
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for CAN_communist_revolution
				set_politics = { ruling_party = communism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		AST = {
			if = {
				limit = { has_game_rule = { rule = SAH_ast_scenario_start_rule option = never_gallipoli_again } }
				# Rewards for AST_support_the_centre_party
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for AST_empower_the_workers
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for AST_the_communist_party_of_australia
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ast_scenario_start_rule option = australia_first } }
				# Rewards for AST_support_the_centre_party
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for AST_the_communist_party_of_australia
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for AST_empower_the_workers
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for AST_the_australia_first_movement
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_ast_scenario_start_rule option = communist_australia } }
				# Rewards for AST_support_the_centre_party
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for AST_empower_the_workers
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for AST_the_communist_party_of_australia
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for AST_workers_revolution
				set_politics = { ruling_party = communism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		SAF = {
			if = {
				limit = { has_game_rule = { rule = SAH_saf_scenario_start_rule option = appeasement } }
				# Rewards for SAF_abandon_westminster
				add_autonomy_ratio = { value = 0.5 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_saf_scenario_start_rule option = ossewabrandwag } }
				# Rewards for SAF_abandon_westminster
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for SAF_support_the_ossewabrandwag
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.2 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_saf_scenario_start_rule option = anti_colonialist } }
				# Rewards for SAF_abandon_westminster
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for SAF_anti_colonialist_crusade
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.2 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		RAJ = {
			if = {
				limit = { has_game_rule = { rule = SAH_raj_scenario_start_rule option = provincial_elections } }
				# Rewards for RAJ_indian_national_congress
				add_autonomy_ratio = { value = 0.5 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_raj_scenario_start_rule option = quit_india } }
				# Rewards for RAJ_indian_national_congress
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for RAJ_quit_india_movement
				add_stability = -0.05
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_raj_scenario_start_rule option = indian_national_army } }
				# Rewards for RAJ_indian_national_congress
				add_autonomy_ratio = { value = 0.5 }
				# Rewards for RAJ_quit_india_movement
				add_stability = -0.05
				# Rewards for RAJ_indian_national_army
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.2 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		ROM = {
			if = {
				limit = { has_game_rule = { rule = SAH_rom_scenario_start_rule option = preserve_romania } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_rom_scenario_start_rule option = align_axis } }
				# Rewards for ROM_the_royal_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rewards for ROM_align_axis
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_rom_scenario_start_rule option = balkan_dominance } }
				# Rewards for ROM_the_royal_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rewards for ROM_the_balkan_dominance
				create_faction = "The Balkan Entente"
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		YUG = {
			if = {
				limit = { has_game_rule = { rule = SAH_yug_scenario_start_rule option = regency_council } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_yug_scenario_start_rule option = tripartite_pact } }
				# Rewards for YUG_sign_the_tripartite_pact
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_yug_scenario_start_rule option = communist_partisans } }
				# Rewards for YUG_the_communist_partisans
				set_politics = { ruling_party = communism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_yug_scenario_start_rule option = western_focus } }
				# Rewards for YUG_western_focus
				if = { limit = { date < 1938.1.1 }
            random_owned_controlled_state = { limit = { free_building_slots = { building = industrial_complex size > 0 } } add_building_construction = { type = industrial_complex level = 2 instant_build = yes } }
						}
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		GRE = {
			if = {
				limit = { has_game_rule = { rule = SAH_gre_scenario_start_rule option = metaxas } }
				# Rewards for GRE_the_venizelist_movement
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for GRE_the_metaxas_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_gre_scenario_start_rule option = megali_idea } }
				# Rewards for GRE_the_venizelist_movement
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for GRE_the_metaxas_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rewards for GRE_the_4th_of_august_regime
				add_stability = 0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_gre_scenario_start_rule option = venizelist } }
				# Rewards for GRE_the_metaxas_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rewards for GRE_the_venizelist_movement
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		TUR = {
			if = {
				limit = { has_game_rule = { rule = SAH_tur_scenario_start_rule option = peace_at_home } }
				# Rewards for TUR_multi_party_system
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for TUR_peace_at_home
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_tur_scenario_start_rule option = multi_party } }
				# Rewards for TUR_peace_at_home
				set_politics = { ruling_party = neutrality }
				# Rewards for TUR_multi_party_system
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		MEX = {
			if = {
				limit = { has_game_rule = { rule = SAH_mex_scenario_start_rule option = prm } }
				# Rewards for MEX_support_the_synarchists
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_mex_scenario_start_rule option = gold_shirts } }
				# Rewards for MEX_support_the_synarchists
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for MEX_the_gold_shirts
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_mex_scenario_start_rule option = hispanic_alliance } }
				# Rewards for MEX_support_the_synarchists
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for MEX_the_gold_shirts
				set_politics = { ruling_party = fascism }
				# Rewards for MEX_hispanic_alliance
				create_faction = "The Hispanic Alliance"
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		BRA = {
			if = {
				limit = { has_game_rule = { rule = SAH_bra_scenario_start_rule option = estado_novo } }
				# Rewards for BRA_the_1937_elections
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for BRA_the_aln_revolt
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for BRA_integralist_uprising
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_bra_scenario_start_rule option = integralist } }
				# Rewards for BRA_the_1937_elections
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for BRA_the_aln_revolt
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for BRA_integralist_uprising
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_bra_scenario_start_rule option = aln_revolt } }
				# Rewards for BRA_the_1937_elections
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for BRA_integralist_uprising
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for BRA_the_aln_revolt
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		SPR = {
			if = {
				limit = { has_game_rule = { rule = SAH_spr_scenario_start_rule option = popular_front } }
				# Rewards for SPR_the_nationalist_uprising
				set_politics = { ruling_party = fascism }
				# Rewards for SPR_the_popular_front
				set_politics = { ruling_party = communism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_spr_scenario_start_rule option = falange } }
				# Rewards for SPR_the_popular_front
				set_politics = { ruling_party = communism }
				# Rewards for SPR_the_nationalist_uprising
				set_politics = { ruling_party = fascism }
				# Rewards for SPR_the_falange
				add_stability = 0.1
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_spr_scenario_start_rule option = anti_fascist_unity } }
				# Rewards for SPR_the_nationalist_uprising
				set_politics = { ruling_party = fascism }
				# Rewards for SPR_the_popular_front
				set_politics = { ruling_party = communism }
				# Rewards for SPR_anti_fascist_unity
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		HOL = {
			if = {
				limit = { has_game_rule = { rule = SAH_hol_scenario_start_rule option = gateway_to_europe } }
				# Rewards for HOL_the_nsb
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for HOL_form_the_new_government
				set_politics = { ruling_party = democratic }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_hol_scenario_start_rule option = anton_mussert } }
				# Rewards for HOL_form_the_new_government
				set_politics = { ruling_party = democratic }
				# Rewards for HOL_the_nsb
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for HOL_anton_mussert
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		SWE = {
			if = {
				limit = { has_game_rule = { rule = SAH_swe_scenario_start_rule option = per_albin_bus } }
				# Rewards for SWE_the_right_wing_coalition
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for SWE_sveriges_kommunistiska_parti
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_swe_scenario_start_rule option = svea_rike } }
				# Rewards for SWE_sveriges_kommunistiska_parti
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for SWE_the_right_wing_coalition
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for SWE_the_svea_rike
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_swe_scenario_start_rule option = socialist_republic } }
				# Rewards for SWE_the_right_wing_coalition
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for SWE_sveriges_kommunistiska_parti
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for SWE_socialist_republic_of_sweden
				set_politics = { ruling_party = communism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		ARG = {
			if = {
				limit = { has_game_rule = { rule = SAH_arg_scenario_start_rule option = infamous_decade } }
				# Rewards for ARG_the_concordancia_government
				set_politics = { ruling_party = neutrality }
            add_stability = -0.05
			}
			if = {
				limit = { has_game_rule = { rule = SAH_arg_scenario_start_rule option = radical_civic } }
				# Rewards for ARG_the_radical_civic_union
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_arg_scenario_start_rule option = peronist } }
				# Rewards for ARG_the_peronist_movement
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		AUS = {
			if = {
				limit = { has_game_rule = { rule = SAH_aus_scenario_start_rule option = fatherland_front } }
				# Rewards for AUS_the_social_democrats
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for AUS_the_fatherland_front
				set_politics = { ruling_party = neutrality }
			}
			if = {
				limit = { has_game_rule = { rule = SAH_aus_scenario_start_rule option = austrofascism } }
				# Rewards for AUS_the_social_democrats
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for AUS_the_fatherland_front
				set_politics = { ruling_party = neutrality }
				# Rewards for AUS_austrofascism
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_aus_scenario_start_rule option = social_democrats } }
				# Rewards for AUS_the_fatherland_front
				set_politics = { ruling_party = neutrality }
				# Rewards for AUS_the_social_democrats
				add_popularity = { ideology = democratic popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		BEL = {
			if = {
				limit = { has_game_rule = { rule = SAH_bel_scenario_start_rule option = catholic_bloc } }
				# Rewards for BEL_the_rexist_party
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for BEL_the_catholic_bloc
				set_politics = { ruling_party = democratic }
			}
			if = {
				limit = { has_game_rule = { rule = SAH_bel_scenario_start_rule option = liberal_party } }
				# Rewards for BEL_the_rexist_party
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for BEL_the_catholic_bloc
				set_politics = { ruling_party = democratic }
				# Rewards for BEL_the_liberal_party
				add_stability = 0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_devastating_recovery
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_bel_scenario_start_rule option = rexist_party } }
				# Rewards for BEL_the_catholic_bloc
				set_politics = { ruling_party = democratic }
				# Rewards for BEL_the_rexist_party
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_devastating_recovery
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
			}
		}
		BUL = {
			if = {
				limit = { has_game_rule = { rule = SAH_bul_scenario_start_rule option = tsars_dictatorship } }
				# Rewards for BUL_the_fatherland_front
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for BUL_zveno
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for BUL_the_tsars_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_bul_scenario_start_rule option = zveno } }
				# Rewards for BUL_the_fatherland_front
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for BUL_the_tsars_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rewards for BUL_zveno
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_bul_scenario_start_rule option = fatherland_front } }
				# Rewards for BUL_the_tsars_dictatorship
				set_politics = { ruling_party = neutrality }
				# Rewards for BUL_zveno
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for BUL_the_fatherland_front
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
			}
		}
		CHI = {
			if = {
				limit = { has_game_rule = { rule = SAH_chi_scenario_start_rule option = historical } }
				# Rewards for CHI_three_principles_of_the_people
				add_ideas = CHI_three_principles_idea
			}
			if = {
				limit = { has_game_rule = { rule = SAH_chi_scenario_start_rule option = welfare } }
				# Rewards for CHI_three_principles_of_the_people
				add_ideas = CHI_three_principles_idea
				# Rewards for CHI_welfare
				add_stability = 0.1
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		CHL = {
			if = {
				limit = { has_game_rule = { rule = SAH_chl_scenario_start_rule option = popular_front } }
				# Rewards for CHL_the_nacistas
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_chl_scenario_start_rule option = nacistas } }
				# Rewards for CHL_the_nacistas
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		COG = {
			if = {
				limit = { has_game_rule = { rule = SAH_cog_scenario_start_rule option = force_publique } }
				# Rewards for COG_the_evolues
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for COG_the_force_publique
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_cog_scenario_start_rule option = evolues } }
				# Rewards for COG_the_force_publique
				set_politics = { ruling_party = neutrality }
				# Rewards for COG_the_evolues
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		CZE = {
			if = {
				limit = { has_game_rule = { rule = SAH_cze_scenario_start_rule option = political_direction } }
				# Rewards for CZE_fascist_direction
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for CZE_communist_direction
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_cze_scenario_start_rule option = fascist_direction } }
				# Rewards for CZE_communist_direction
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for CZE_fascist_direction
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_cze_scenario_start_rule option = communist_direction } }
				# Rewards for CZE_fascist_direction
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for CZE_communist_direction
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		DEN = {
			if = {
				limit = { has_game_rule = { rule = SAH_den_scenario_start_rule option = social_democrats } }
				# Rewards for DEN_the_conservative_peoples_party
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_den_scenario_start_rule option = conservative } }
				# Rewards for DEN_the_conservative_peoples_party
				add_popularity = { ideology = neutrality popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		EGY = {
			if = {
				limit = { has_game_rule = { rule = SAH_egy_scenario_start_rule option = anglo_treaty } }
				# Rewards for EGY_the_wafd_party
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for EGY_the_anglo_egyptian_treaty
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_egy_scenario_start_rule option = wafd_party } }
				# Rewards for EGY_the_anglo_egyptian_treaty
				set_politics = { ruling_party = neutrality }
				# Rewards for EGY_the_wafd_party
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		EST = {
			if = {
				limit = { has_game_rule = { rule = SAH_est_scenario_start_rule option = era_of_silence } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_est_scenario_start_rule option = vaps_movement } }
				# Rewards for EST_the_vaps_movement
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		ETH = {
			if = {
				limit = { has_game_rule = { rule = SAH_eth_scenario_start_rule option = emperor } }
				# Rewards for ETH_the_emperor
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		FIN = {
			if = {
				limit = { has_game_rule = { rule = SAH_fin_scenario_start_rule option = finnish_model } }
				# Rewards for FIN_the_lapua_movement
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for FIN_the_red_guards
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_fin_scenario_start_rule option = lapua_movement } }
				# Rewards for FIN_the_red_guards
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for FIN_the_lapua_movement
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_fin_scenario_start_rule option = red_guards } }
				# Rewards for FIN_the_lapua_movement
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for FIN_the_red_guards
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		HUN = {
			if = {
				limit = { has_game_rule = { rule = SAH_hun_scenario_start_rule option = renounce_trianon } }
				# Rewards for HUN_a_king_for_our_people
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for HUN_the_communist_party
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rewards for HUN_renounce_the_treaty_of_trianon
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_hun_scenario_start_rule option = king } }
				# Rewards for HUN_renounce_the_treaty_of_trianon
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for HUN_the_communist_party
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rewards for HUN_a_king_for_our_people
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_hun_scenario_start_rule option = communist } }
				# Rewards for HUN_renounce_the_treaty_of_trianon
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for HUN_a_king_for_our_people
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for HUN_the_communist_party
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_hun_scenario_start_rule option = habsburg } }
				# Rewards for HUN_renounce_the_treaty_of_trianon
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for HUN_a_king_for_our_people
				set_politics = { ruling_party = democratic elections_allowed = yes }
				# Rewards for HUN_the_communist_party
				set_politics = { ruling_party = communism elections_allowed = no }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		IRQ = {
			if = {
				limit = { has_game_rule = { rule = SAH_irq_scenario_start_rule option = hashemite } }
				# Rewards for IRQ_the_golden_square
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for IRQ_the_hashemite_kingdom
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_irq_scenario_start_rule option = golden_square } }
				# Rewards for IRQ_the_hashemite_kingdom
				set_politics = { ruling_party = neutrality }
				# Rewards for IRQ_the_golden_square
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		LAT = {
			if = {
				limit = { has_game_rule = { rule = SAH_lat_scenario_start_rule option = ulmainis } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_lat_scenario_start_rule option = perkonkrusts } }
				# Rewards for LAT_the_perkonkrusts
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		LIT = {
			if = {
				limit = { has_game_rule = { rule = SAH_lit_scenario_start_rule option = smetona } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_lit_scenario_start_rule option = iron_wolf } }
				# Rewards for LIT_the_iron_wolf
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		MAN = {
			if = {
				limit = { has_game_rule = { rule = SAH_man_scenario_start_rule option = obedience } }
				# Rewards for MAN_assertiveness
				set_politics = { ruling_party = neutrality }
				# Rewards for MAN_the_democratic_party
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for MAN_the_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for MAN_obedience
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_man_scenario_start_rule option = assertiveness } }
				# Rewards for MAN_the_democratic_party
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for MAN_the_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for MAN_obedience
				set_politics = { ruling_party = fascism }
				# Rewards for MAN_assertiveness
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_man_scenario_start_rule option = democratic } }
				# Rewards for MAN_assertiveness
				set_politics = { ruling_party = neutrality }
				# Rewards for MAN_obedience
				set_politics = { ruling_party = fascism }
				# Rewards for MAN_the_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for MAN_the_democratic_party
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_man_scenario_start_rule option = communist } }
				# Rewards for MAN_assertiveness
				set_politics = { ruling_party = neutrality }
				# Rewards for MAN_the_democratic_party
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for MAN_obedience
				set_politics = { ruling_party = fascism }
				# Rewards for MAN_the_communist_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		NOR = {
			if = {
				limit = { has_game_rule = { rule = SAH_nor_scenario_start_rule option = nygaardsvold } }
				# Rewards for NOR_the_communist_party
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for NOR_the_right_wing_coalition
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_nor_scenario_start_rule option = quisling } }
				# Rewards for NOR_the_communist_party
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for NOR_the_right_wing_coalition
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for NOR_quisling_government
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_nor_scenario_start_rule option = communist } }
				# Rewards for NOR_the_right_wing_coalition
				add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for NOR_the_communist_party
				add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		NZL = {
			if = {
				limit = { has_game_rule = { rule = SAH_nzl_scenario_start_rule option = labour } }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		PAR = {
			if = {
				limit = { has_game_rule = { rule = SAH_par_scenario_start_rule option = febrerista } }
				# Rewards for PAR_the_febrerista_revolution
				set_politics = { ruling_party = fascism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		PER = {
			if = {
				limit = { has_game_rule = { rule = SAH_per_scenario_start_rule option = pahlavi } }
				# Rewards for PER_the_tudeh_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for PER_the_pahlavi_dynasty
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_per_scenario_start_rule option = tudeh } }
				# Rewards for PER_the_pahlavi_dynasty
				set_politics = { ruling_party = neutrality }
				# Rewards for PER_the_tudeh_party
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		PHI = {
			if = {
				limit = { has_game_rule = { rule = SAH_phi_scenario_start_rule option = commonwealth } }
				# Rewards for PHI_the_hukbalahap
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for PHI_the_ganap_party
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for PHI_the_commonwealth
				set_politics = { ruling_party = democratic }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_phi_scenario_start_rule option = ganap } }
				# Rewards for PHI_the_commonwealth
				set_politics = { ruling_party = democratic }
				# Rewards for PHI_the_hukbalahap
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for PHI_the_ganap_party
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_phi_scenario_start_rule option = hukbalahap } }
				# Rewards for PHI_the_commonwealth
				set_politics = { ruling_party = democratic }
				# Rewards for PHI_the_ganap_party
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for PHI_the_hukbalahap
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		POR = {
			if = {
				limit = { has_game_rule = { rule = SAH_por_scenario_start_rule option = estado_novo } }
				# Rewards for POR_the_popular_front
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for POR_the_estado_novo
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_por_scenario_start_rule option = popular_front } }
				# Rewards for POR_the_estado_novo
				set_politics = { ruling_party = neutrality }
				# Rewards for POR_the_popular_front
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		PRC = {
			if = {
				limit = { has_game_rule = { rule = SAH_prc_scenario_start_rule option = maoism } }
				# Rewards for PRC_social_democracy
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for PRC_orthodox_marxism
				add_popularity = { ideology = communism popularity = 0.1 }
            add_stability = 0.05
				# Rewards for PRC_maoism
				set_politics = { ruling_party = communism }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_prc_scenario_start_rule option = orthodox_marxism } }
				# Rewards for PRC_social_democracy
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for PRC_maoism
				set_politics = { ruling_party = communism }
				# Rewards for PRC_orthodox_marxism
				add_popularity = { ideology = communism popularity = 0.1 }
            add_stability = 0.05
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_prc_scenario_start_rule option = social_democracy } }
				# Rewards for PRC_maoism
				set_politics = { ruling_party = communism }
				# Rewards for PRC_orthodox_marxism
				add_popularity = { ideology = communism popularity = 0.1 }
            add_stability = 0.05
				# Rewards for PRC_social_democracy
				set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		SWI = {
			if = {
				limit = { has_game_rule = { rule = SAH_swi_scenario_start_rule option = federal_council } }
				# Rewards for SWI_the_federal_council
				set_politics = { ruling_party = democratic }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		URU = {
			if = {
				limit = { has_game_rule = { rule = SAH_uru_scenario_start_rule option = colorado } }
				# Rewards for URU_the_colorado_party
				set_politics = { ruling_party = democratic }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		WRL = {
			if = {
				limit = { has_game_rule = { rule = SAH_wrl_scenario_start_rule option = nationalists } }
				# Rewards for WRL_democratic_reforms
				set_politics = { ruling_party = democratic }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for WRL_military_dictatorship
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for WRL_cooperate_with_the_communists
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rewards for WRL_cooperate_with_the_nationalists
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
			if = {
				limit = { has_game_rule = { rule = SAH_wrl_scenario_start_rule option = communists } }
				# Rewards for WRL_democratic_reforms
				set_politics = { ruling_party = democratic }
            add_popularity = { ideology = democratic popularity = 0.1 }
				# Rewards for WRL_military_dictatorship
				set_politics = { ruling_party = fascism }
            add_popularity = { ideology = fascism popularity = 0.1 }
				# Rewards for WRL_cooperate_with_the_nationalists
				set_politics = { ruling_party = neutrality }
				# Rewards for WRL_cooperate_with_the_communists
				set_politics = { ruling_party = communism }
            add_popularity = { ideology = communism popularity = 0.1 }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
		AFG = {
			if = {
				limit = { has_game_rule = { rule = SAH_afg_scenario_start_rule option = barakzai } }
				# Rewards for AFG_the_barakzai_dynasty
				set_politics = { ruling_party = neutrality }
				# Rebuilding State Tier logic
				if = {
					limit = { date < 1938.1.1 }
					add_ideas = SAH_prolonged_rebuilding
				}
				if = {
					limit = { date > 1938.1.1 }
					add_ideas = SAH_stabilized_regime
				}
			}
		}
	}



	option = {
		name = SAH_init.1.a
	}
}
