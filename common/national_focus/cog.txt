focus_tree = {
    id = congolese_focus

    country = {
        factor = 0
        modifier = {
            add = 10
            tag = COG
        }
    }

    default = no

    focus = {
        id = COG_the_force_publique
		bypass = {
			OR = {
				has_start_date > 1939.08.14
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = evolues }
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = force_publique }
			}
			OR = {
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = force_publique }
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = evolues }
			}
		}

        icon = GFX_goal_generic_suppress_resistance
        x = 4
        y = 0
        cost = 10
        mutually_exclusive = { focus = COG_the_evolues }

                ai_will_do = {
            base = 10
            modifier = {
                add = 90
                is_historical_focus_on = yes
            }
            modifier = {
                factor = 1.25
                has_idea_with_trait = neutrality_minister
            }
            modifier = {
                factor = 2.0
                any_neighbor_country = { has_government = neutrality is_major = yes }
            }
        }


        completion_reward = {
            add_political_power = 120
            set_politics = { ruling_party = neutrality }
        }
    }

    focus = {
        id = COG_the_evolues
		bypass = {
			OR = {
				has_start_date > 1939.08.14
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = evolues }
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = force_publique }
			}
			OR = {
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = force_publique }
				has_game_rule = { rule = SAH_cog_scenario_start_rule option = evolues }
			}
		}

        icon = GFX_goal_generic_peaceful_treaty
        x = 8
        y = 0
        cost = 10
        mutually_exclusive = { focus = COG_the_force_publique }

                ai_will_do = {
            base = 10
            modifier = {
                factor = 0
                is_historical_focus_on = yes
            }
            modifier = {
                factor = 1.25
                has_idea_with_trait = democratic_minister
            }
            modifier = {
                factor = 2.0
                any_neighbor_country = { has_government = democratic is_major = yes }
            }
        }


        completion_reward = {
            add_political_power = 50
            set_politics = { ruling_party = democratic elections_allowed = yes }
            add_popularity = { ideology = democratic popularity = 0.1 }
        }
    }

    focus = {
        id = COG_develop_katanga
        icon = GFX_goal_generic_production
        x = 6
        y = 2
        cost = 10

        ai_will_do = {
            base = 1
        }

        completion_reward = {
            random_owned_controlled_state = { limit = { free_building_slots = { building = industrial_complex size > 0 } } add_building_construction = { type = industrial_complex level = 2 instant_build = yes } }
        }
    }

}